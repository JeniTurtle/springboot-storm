mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.jouryu.model

context.listener.classes=com.jouryu.common.ApplicationEventListener

## 开发/生产环境分别对应dev/prod，可以自由定义
spring.profiles.active=dev

# logging.level.root=DEBUG
logging.config=classpath:log4j2.properties